# Safe Mode — device QA (2026-05-21)

**Branch:** `claude/add-gym-mode-2elAf` (merged) + `fix/premises-map-tiles` (PR #390 in flight)
**Build:** Phase 1 (schema + portal + `/v/{slug}`) + Phase 2 (mobile capture + native Swift `SafeModeProcessor`) + PR #390 fixes (map tiles, Nominatim, disable hint, 42702 list RPC).
**Skipping section A** (schema sanity) — testing from B onward.

**URLs (merged to `staging`):**
- Portal (sections B + C, I): https://staging.manage.homefit.studio
- Web player (section D): https://staging.session.homefit.studio
- Mobile (sections E + F + G + H): installed on iPhone CHM via `install-device.sh staging`

How to use: tick each box as you go. Drop fail notes in chat ("9 fails — no polygon appears"). Passing items get struck through inline. New requirements that emerge get appended as numbered items in **section I** onward — never re-numbered, never inserted mid-list.

---

## B. Portal — Premises page

- [x] ~~6. Dashboard renders a new **Premises** tile next to Clients / Network / Credits / Members. Headline reads `No premises` or `N sites · N enforce Safe Mode`.~~
- [x] ~~7. Tap the Premises tile. URL is `/premises?practice=<uuid>`. Header reads `Premises` with spec-explainer copy.~~
- [x] ~~8. Tap **+ Add premises**. Modal opens. Map renders centred on current GPS or Cape Town fallback.~~
- [x] ~~9. Tap 3 points → coral polygon with numbered vertex pins. Vertex counter `3 / 12`. Area in m² (km² above 10,000 m²).~~
- [ ] 10. Drag a vertex pin → polygon reshapes live, area updates.
- [ ] 11. Right-click a vertex pin → vertex removes. Counter decrements.
- [x] ~~12. Tap **Undo** → last placed vertex removes.~~
- [x] ~~13. Tap **Clear** → polygon disappears, counters reset.~~
- [x] ~~14. Try to tap a 13th point → no new vertex placed.~~
- [x] ~~15. Draw a tiny polygon (~1 m²). Save → error `Polygon too small (min 25 m²)`.~~
- [x] ~~16. Draw a sensible polygon, fill Name, leave Address empty, Safe Mode checked. Save → row appears with `Safe Mode on` coral badge.~~
- [x] ~~17. Row shows m²/km² area + `GPS` signal type chip.~~
- [ ] 18. Edit row → modal opens populated. Toggle Safe Mode OFF, Save → row updates with `Registered only` grey badge.
- [x] ~~19. Delete row → confirm prompt → row disappears.~~
- [x] ~~20. Refresh → deleted row stays gone.~~

## C. Portal — Public profile section

- [x] ~~21. `Public profile` section present with URL preview `session.homefit.studio/v/your-slug`.~~
- [x] ~~22. Slug pre-populated from practice name. Edit; invalid chars stripped live.~~
- [x] ~~23. Slug starting with hyphen → error `Slug must be 3–40 lowercase letters, digits, or hyphens.`~~
- [x] ~~24. Blurb >280 chars → counter red, `Save profile` disables.~~
- [x] ~~25. List OFF + Save → `Saved ✓`.~~
- [x] ~~26. List ON without a slug → error `Pick a slug before listing in the directory.`~~
- [x] ~~27. Fill slug + check List + Save → saved.~~
- [x] ~~28. Sign in as non-owner member. Premises list still loads; public profile section is read-only.~~
- [x] ~~29. As practitioner, upsert a premises → succeeds.~~
- [x] ~~30. Sign back in as owner.~~

## D. Web player — `/v/{slug}`

- [ ] 31. Open `https://homefit-web-player-git-claude-158c12-carlheinmosterts-projects.vercel.app/v/<slug>` on mobile Safari. Practice name centred, blurb below, coral logo placeholder with initials.
- [ ] 32. Premises list shows name + address + coral `Safe Mode` badge for enforced ones.
- [ ] 33. Tap **Open in maps →** → opens OpenStreetMap on centroid.
- [ ] 34. Tap **Report this practice** → modal opens with premises dropdown.
- [ ] 35. Submit with empty reason → error `Add a reason before sending.`
- [ ] 36. Submit with real reason → `Sent ✓`, modal auto-closes.
- [ ] 37. Verify: `select * from public.premises_reports order by created_at desc limit 5;` — latest row matches.
- [ ] 38. Open `/v/nonexistent-slug-12345` → renders `Practice not found`.
- [ ] 39. Toggle `List in the directory` OFF, refresh → `Practice not found`. Toggle back ON → profile returns.
- [ ] 40. Send the `/v/{slug}` URL via WhatsApp. Link preview shows practice name + blurb.

## E. Mobile — Safe Mode banner (inside polygon)

- [ ] 41. Stand inside an enforced polygon (or simulate via Xcode → Debug → Simulate Location).
- [ ] 42. Open a client → **+ New session** → swipe to **Camera**.
- [ ] 43. First time: iOS permission dialog. Tap **Allow**.
- [ ] 44. Coral banner appears: `Safe Mode active · <Premises name>` with shield icon.
- [ ] 45. Capture a short video (~3s) with yourself in frame. Studio thumbnail peek shows capture.
- [ ] 46. Walk outside the polygon (or simulate). Capture another video. Banner stays visible (session-sticky grace).
- [ ] 47. Swipe back to Studio. Banner still visible.
- [ ] 48. Exit the session. Open a session NOT created in a polygon. Banner does NOT appear.

## F. Mobile — Vision compositing

- [ ] 49. Locate `<exercise-id>_safe.mp4` in app's `Documents/converted/` directory.
- [ ] 50. Play it. Client passes through normally; **other people are flat coral silhouette**; background unchanged.
- [ ] 51. Xcode device log: `[VideoConverter] safe writer attached at ...` → `safe writer started` → `safe finishWriting completed — safeFrames=N`.
- [ ] 52. `<exercise-id>_line.mp4` unchanged from non-Safe-Mode capture.
- [ ] 53. `<exercise-id>_segmented.mp4` unchanged — bystanders still pop in B&W.
- [ ] 54. Record pointing at a wall (no humans). Log shows `safeFrames=0`, `_safe.mp4` empty/missing. Line + segmented + mask still wrote.

## G. Known gaps — should FAIL (confirm the failure mode)

- [ ] 55. Publish the session from step 45. Open the plan URL. B&W and Original treatments **still show un-blurred bystanders**. → Confirms missing upload-swap.
- [ ] 56. Portal `/audit` for that publish does NOT carry `safe_mode_active` / `captured_in_premises_id`. → Confirms missing audit-stamping.
- [ ] 57. Photo capture inside the polygon: line-drawing JPG unchanged from non-Safe-Mode photo. → Confirms photo parity not wired.

## H. Regression sweep

- [ ] 58. Capture a video **outside** any polygon. No banner. Log: `safeOutputWritten=false`, `safeFrames=0`. Other outputs unchanged.
- [ ] 59. Publish that plan. Web player plays back B&W and Original normally.
- [ ] 60. Toggle iOS location permission OFF. Open a session inside a polygon. Banner does NOT appear. Capture works normally.
- [ ] 61. Existing portal pages (`/clients`, `/credits`, `/audit`, `/members`, `/network`) all still load.
- [ ] 62. Existing `/p/{planId}` plan pages still render. Lobby still works. Three-treatment selector works. Start workout works.

## I. PR #390 follow-ups — premises editor (appended 2026-05-21 after item-8 debug)

- [ ] 63. Address field accepts free-text typing. Type `Virgin Active Sandton` → dropdown of ~5 South African results appears within ~1 second under the input.
- [ ] 64. Click a Nominatim dropdown result → map flies to that location (0.6 s animation), address input populates with the full `display_name`. Dropdown closes.
- [ ] 65. Search `Big Ben` → dropdown shows **no results** (ZA bias via `countrycodes=za`).
- [ ] 66. Rapid type-and-delete (`London`, backspace x6, `Cape`, backspace x4) → no console 429 errors; only the final query's results render (AbortController defends).
- [ ] 67. With Save button disabled, a grey hint appears to the left of the button explaining the first failing gate (e.g. `Add a name to enable Save.` / `Place at least 3 vertices (currently 1).` / `Polygon too small (need ≥ 25 m², currently 12 m²).`). Hint disappears once the form passes all gates.
- [ ] 68. **Pre-existing rows reappear after PR #390 deploy.** The two rows already on staging (`sss` + `asdasdsa`, both created 2026-05-21 ~12:00 UTC on practice `carlhein Practice`) become visible in the list as soon as the 42702 hotfix migration applies. **No re-creation needed.**
- [ ] 69. Refresh `/premises` → all created rows persist (smoke check the list RPC after each save).
- [ ] 70. **PR #391** — `curl -sI https://staging.manage.homefit.studio/premises` returns a `content-security-policy` header that includes `https://*.tile.openstreetmap.org` in `img-src` and `https://nominatim.openstreetmap.org` in `connect-src`. (CSP was the real reason items 8 + 63 failed after PR #390 deploy; PR #390's CSS-and-invalidateSize work was defensive hardening but not the root cause.)
- [ ] 71. Browser DevTools console at `/premises` → no `Refused to load … because it violates the following Content Security Policy directive` errors when the editor modal opens or the address field is typed in.
