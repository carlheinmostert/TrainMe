# 2026-05-21 — Safe Mode + Public Profile v2 + Premises wave

Consolidated test script for the staging tip at `a7d39c8`. Covers everything merged on `staging` between PR #389 and PR #402.

Open this file in the Claude Code preview pane (Cmd+Shift+V). Check items off as you go; strike-through (`- [x] ~~text~~`) for items that pass; leave `- [ ]` for items still pending; flip to `- [ ] FAIL — note` for failures.

## Table of contents

- [Pre-flight](#pre-flight) — items 1-5
- [Section B — Portal /premises page](#section-b--portal-premises-page) — items 6-20
- [Section C — Portal /premises public-profile block (legacy)](#section-c--portal-premises-public-profile-block-legacy) — items 21-30
- [Section D — Web player /v/{slug}](#section-d--web-player-vslug) — items 31-40
- [Section E — Mobile Safe Mode banner inside polygon](#section-e--mobile-safe-mode-banner-inside-polygon) — items 41-48
- [Section F — Mobile Vision compositing](#section-f--mobile-vision-compositing) — items 49-54
- [Section G — Should-FAIL today, must-PASS after Safe Mode completion lands](#section-g--should-fail-today-must-pass-after-safe-mode-completion-lands) — items 55-57
- [Section H — Regression sweep](#section-h--regression-sweep) — items 58-62
- [Section I — PR #390 portal follow-ups](#section-i--pr-390-portal-follow-ups) — items 63-71
- [Section J — Public Profile v2](#section-j--public-profile-v2) — items 72-82
- [Section K — Safe Mode completion](#section-k--safe-mode-completion) — items 83-92

## Pre-flight

- [ ] **1.** Hard-refresh `https://staging.manage.homefit.studio` and confirm header shows the staging build SHA chip.
- [ ] **2.** Hard-refresh `https://staging.session.homefit.studio` and confirm the footer build SHA matches.
- [ ] **3.** iPhone CHM: run `./install-device.sh staging` and confirm app launches on the Clients screen with the staging tint.
- [ ] **4.** Confirm `pubspec.yaml` shows `1.0.0+4` (bump to `+5` before TestFlight upload if Safe Mode completion lands).
- [ ] **5.** Sign in as `qa@homefit.studio` (credentials in `.env.test`) and confirm the QA Test Practice loads with seeded credits.

## Section B — Portal /premises page

Most of this section passed during yesterday's wave. Re-verify only if a regression is suspected. Item 11 is the only one untested.

- [ ] ~~**6.** /premises page loads, shows existing premises list with polygon counts.~~ (passed 2026-05-21)
- [ ] ~~**7.** "Add premises" opens the editor with empty polygon + address autocomplete.~~ (passed)
- [ ] ~~**8.** Nominatim address search returns results outside South Africa (no strict country filter).~~ (passed)
- [ ] ~~**9.** Photon parallel geocoder returns deduped results alongside Nominatim with source labels.~~ (passed)
- [ ] ~~**10.** Click a search result → map pans to it and a single pin is placed.~~ (passed)
- [ ] **11.** Right-click an existing polygon vertex → vertex is removed (UNTESTED).
- [ ] ~~**12.** Drag a vertex across the map → polygon edge follows without snapping back.~~ (passed)
- [ ] ~~**13.** Switch Street ↔ Satellite layer → zoom level holds, polygon stays in place.~~ (passed)
- [ ] ~~**14.** Esri labels overlay is visible on satellite view.~~ (passed)
- [ ] ~~**15.** "Use my location" button prompts geolocation and pans the map on success.~~ (passed)
- [ ] ~~**16.** Geolocation denied → map stays where it was, no error spam in console.~~ (passed)
- [ ] ~~**17.** Save polygon → row updates with the new vertex count; toast confirms.~~ (passed)
- [ ] ~~**18.** Delete premises → soft-delete with undo SnackBar.~~ (passed)
- [ ] ~~**19.** Disable a premises → row shows the "disabled" hint chip.~~ (passed)
- [ ] ~~**20.** OSM tiles + Nominatim load without CSP errors in DevTools console.~~ (passed)

## Section C — Portal /premises public-profile block (legacy)

This entire section is displaced by Section J once Public Profile v2 lands. Skip unless you want to confirm the block has been REMOVED from /premises.

- [ ] **21.** Open /premises and confirm the "Public profile" `<details>` block is NO LONGER PRESENT.
- [ ] **22.** Public profile fields have all moved to `/public-profile?practice=<uuid>`.
- [ ] ~~**23.** Slug validation rejects uppercase / spaces.~~ (legacy — re-verified in Section J)
- [ ] ~~**24.** Slug ≥ 3 chars enforced.~~ (legacy)
- [ ] ~~**25.** Blurb 280-char hard cap.~~ (legacy)
- [ ] ~~**26.** Blurb counter goes red at 250 chars.~~ (legacy)
- [ ] ~~**27.** "List in directory" toggle persists across reload.~~ (legacy)
- [ ] ~~**28.** Toggle off → /v/{slug} returns "Practice not found".~~ (legacy)
- [ ] ~~**29.** Toggle back on → /v/{slug} renders again with previous content intact.~~ (legacy)
- [ ] ~~**30.** Save flow shows inline confirmation, no SnackBar, no nav redirect.~~ (legacy)

## Section D — Web player /v/{slug}

Open `https://staging.session.homefit.studio/v/<your-slug>` in a fresh browser tab.

- [ ] **31.** Page loads at `staging.session.homefit.studio/v/{slug}` (verified by PR #401 redirect).
- [ ] **32.** Hero shows practice name + tagline (or fallback if unset).
- [ ] **33.** Fine-print paragraph reads cleanly — NO literal `{' '}` JSX-escape characters visible (CRITICAL F-C1 from review).
- [ ] **34.** Premises list shows each polygon with a static map thumbnail.
- [ ] **35.** "What we share" link goes to `/what-we-share` and back works via browser back.
- [ ] **36.** OG meta tags render correctly when the URL is pasted into a WhatsApp draft (preview shows practice name + tagline).
- [ ] **37.** Page is reachable when `public_profile_listed = true` and 404s when false.
- [ ] **38.** Service worker doesn't cache stale public-profile content (hard refresh shows latest edits within ~5s).
- [ ] **39.** Mobile Safari renders without layout shift on first paint.
- [ ] **40.** Lighthouse mobile score ≥ 85 on staging (smoke check, not a hard gate).

## Section E — Mobile Safe Mode banner inside polygon

Carry the iPhone into one of your test polygons (or simulate by using `Debug → Set Location`). Open Camera mode.

- [ ] **41.** Coral "Safe Mode" banner appears at the top of the viewfinder when inside a polygon.
- [ ] **42.** Banner shows the premises name (e.g. "Home practice").
- [ ] **43.** Leaving the polygon → banner disappears within ~3s.
- [ ] **44.** Re-entering → banner reappears.
- [ ] **45.** Banner is visible in both portrait and landscape camera orientations.
- [ ] **46.** Banner does not overlap the shutter button or lens pills.
- [ ] **47.** Banner is preserved across Camera ↔ Studio page swipes (no flicker).
- [ ] **48.** GPS revocation in iOS Settings → banner disappears + a fallback chip appears explaining why.

## Section F — Mobile Vision compositing

Capture a 5-10 second video of yourself with a second person in frame. Long-press shutter to record.

- [ ] **49.** During capture, no visible delay or stutter compared to non-Safe-Mode capture.
- [ ] **50.** After capture, the converted video shows YOU clearly + the second person rendered as a coral silhouette.
- [ ] **51.** Background scene is preserved (not blurred away).
- [ ] **52.** Edges of the silhouette are clean (no halos, no flicker between frames).
- [ ] **53.** Audio is preserved and in sync.
- [ ] **54.** Hero thumbnail on the Studio card shows the composited frame (not the raw).

## Section G — Should-FAIL today, must-PASS after Safe Mode completion lands

These items are KNOWN to fail until PR for Safe Mode completion (specs at `docs/specs/2026-05-21-safe-mode-completion-design.md`) lands. Re-run after merge.

- [ ] **55.** Publish a Safe Mode capture → cloud `raw-archive/{practice_id}/{plan_id}/{exercise_id}.mp4` contains the SAFE variant (coral silhouettes), not the raw. (Today: raw is uploaded.)
- [ ] **56.** After publish, `exercises.safe_mode_active` is `true` and `captured_in_premises_id` is populated on the DB row. (Today: NULL.)
- [ ] **57.** Capture a PHOTO inside a polygon → photo Safe Mode pass runs, B&W treatment shows coral silhouette. (Today: photo Safe Mode not implemented.)

## Section H — Regression sweep

Quick sweep that the wave didn't break load-bearing flows. Use any existing test plan.

- [ ] **58.** Publish a non-Safe-Mode video plan → URL works on the web player, treatments switch correctly.
- [ ] **59.** Existing pre-wave plans still play correctly on /p/{planId}.
- [ ] **60.** Three-treatment segmented control on the player works (Line / B&W / Original) for both photos and videos.
- [ ] **61.** Credit balance decrements on publish and audit log entry appears.
- [ ] **62.** Sign out → sign in via magic link still works (Resend SMTP path).

## Section I — PR #390 portal follow-ups

Items deferred from the premises editor PR #390. Verify low-priority polish.

- [ ] **63.** /premises tile on dashboard shows the correct premises count.
- [ ] **64.** Address autocomplete dropdown anchors directly under the input (no offset).
- [ ] **65.** Polygon Edit mode renders existing vertices without a race / flash.
- [ ] **66.** Address search debounce feels right — no spam requests on every keystroke. (Low priority.)
- [ ] **67.** Top-of-page progress bar appears on portal navigation between routes.
- [ ] **68.** "Clear" button in polygon editor clears all vertices.
- [ ] **69.** "Undo" button removes the last vertex.
- [ ] **70.** CSP allows OSM tiles + Nominatim + Photon + Esri without console errors. (Low priority — already verified during editor work.)
- [ ] **71.** Permissions-Policy `geolocation=(self)` allows the geo button to work. (Low priority.)

## Section J — Public Profile v2

`/public-profile?practice=<uuid>` — new top-level page. Owner edits, member views, non-member 404.

- [ ] **72.** Dashboard tile "Public Profile" appears between Clients and Credits, shows configured-state line or "Not configured".
- [ ] **73.** Open `/public-profile` with no fields set → page renders, both `<details>` sections expand on click, hero shows coral fallback + initials.
- [ ] **74.** Set `brand_color` to `#10B981` → live preview strip tints to green immediately; Save → revisit `/v/{slug}` shows green tagline, green hero CTA border, green Safe Mode badges.
- [ ] **75.** Open an existing published plan in the web player → coral elements are now green (active pill, prep countdown, treatment switcher active border, timer chip).
- [ ] **76.** Upload a square PNG logo (256×256) → appears in the hero on `/v/{slug}` AND in the player top-bar within ~1s.
- [ ] **77.** Upload a landscape SVG logo (500×120) → renders correctly in both surfaces.
- [ ] **78.** Fill every contact field (email, WhatsApp, website) → hero shows the website CTA, contact list shows email + WhatsApp rows.
- [ ] **79.** Non-owner member view → all inputs disabled, info note visible, Save buttons hidden. `/v/{slug}` still readable.
- [ ] **80.** Dashboard tile reflects "{N} fields set" correctly after each save.
- [ ] **81.** Deep-link `/public-profile?section=branding` pre-expands Branding section.
- [ ] **82.** Logo upload race protection: pick a second file before the first finishes → final state matches the second file (no flicker, no swallowed error).

## Section K — Safe Mode completion

Re-run Section G items 55-57 after this PR merges, plus the items below.

- [ ] **83.** Capture a video with deliberate Vision failure (cover face, dark lighting) → if miss-rate > 5%, capture is rejected with coral-bordered inline toast "Safe Mode couldn't track everyone — try a steadier shot or better lighting".
- [ ] **84.** Toast auto-dismisses after ~4s. No modal (R-01).
- [ ] **85.** Below-threshold miss-rate (e.g. 2%) → capture proceeds, soft-skip on gap frames is invisible.
- [ ] **86.** SQLite `exercises.safe_raw_file_path` column populated after conversion completes (verify via Settings → Dev → DB browser if available, or via `sqlite3` on simulator).
- [ ] **87.** Kill app mid-publish → relaunch → publish resumes correctly using `safe_raw_file_path` (crash recovery).
- [ ] **88.** Photo Safe Mode capture produces a coral-silhouette frame in B&W treatment + raw archive cloud entry is the safe variant.
- [ ] **89.** `replace_plan_exercises` RPC accepts the two new array params (`p_safe_mode_active`, `p_captured_in_premises_id`) without breaking older callers that pass NULL.
- [ ] **90.** Local archive on device still contains the ORIGINAL raw file (not the safe variant) — confirm via Files.app or the existing re-export path.
- [ ] **91.** Re-publish an exercise that was originally published WITHOUT Safe Mode, then had Safe Mode enabled (polygon added) → cloud raw is replaced with safe.mp4 (verify CRITICAL S-C2 from review).
- [ ] **92.** Photo path: when `safe_raw_file_path` is set in SQLite but missing on disk → publish skips that photo cleanly, no silent fallback to un-blurred original (verify CRITICAL Q-C1 from review).
