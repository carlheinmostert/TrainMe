# Line-drawing tuning LOCKED at v6

The four converter parameters (`edgeThresholdLo=1`, `edgeThresholdHi=0.88`, `lineAlpha=0.96`, `backgroundDim=0.70`) are aesthetic-signoff-locked. Don't tweak without explicit Carl sign-off — the visual signature is load-bearing for brand recognition. Future style variants ship as new versions (v7+), not as edits to v6 defaults.
