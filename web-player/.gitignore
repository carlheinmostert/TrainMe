# Generated by build.sh from Vercel-injected env vars at deploy time.
# Source-of-truth lives in NEXT_PUBLIC_SUPABASE_URL +
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY on the Vercel project. See
# build.sh for the fallback values used when env vars are absent
# (local file:// dev, etc.). api.js falls back to inline prod
# constants when window.HOMEFIT_CONFIG is missing entirely.
config.js
